# TO DO
° Add pets: item displays that follow player (see below), make a leave function to respawn the entity, each 10 seconds schedule for uuid and remove entity, use a scoreboard to know which entity it is (not tags, it's annoying).
Minimum distance=5..
Add particles to the item_display based on tag.

° Special access rooms that are above a certain Y and won't tp players down (make it so the y is checked up to a certain poin, 20 blocks threshold and each layer has its own location check)